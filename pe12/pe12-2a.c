#include <stdio.h>

static int mode;
static double distance;
static double consume_oil;
static int oil_cast;
/*

int set_mode(int mode) {
	switch (mode) {
	case 0:
		break;
	}
}

int get_info(void) {
	printf("Enter distance traveled in kilometers:"), scanf("%d", &distance), printf("Enter fuel consumed in liters:78.9"), scanf("%d", &consume_oil)
}

int pe12(void) {
	printf("Enter 0 for metric mode,1 for us mode:");
	scanf("%d", &mode);
	while (mode >= 0) {
		set_mode(mode);
		get_info();
		show_info();
		printf("Enter 0 for metric mode,1 for us mode:");
		printf("(-1 to quit):");
		scanf("%d", &mode);
	}
}

*/